/*var lobby_sound = Det_svinger_sound;

if game(0) {
    while ( room = cutscene_room) 
      {
      audio_play_sound(lobby_sound,true,true);
     //audio_sound_gain(snd, 0.4, 0);
      }
    } 
    */
